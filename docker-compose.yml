version: "3.8"
 
volumes:
  rabbitmq_data:

services:
 
  #################################
  # Assignment: The Assignment microservice
  #################################
  assignment:
    build:
      context: ./
      dockerfile: assignment.Dockerfile
    image: minhuilee/assignment:esd
    restart: always
    environment:
      dbURL: mysql+mysqlconnector://is213@localhost:3306/assignment
      PYTHONUNBUFFERED: 1
    ports: 
      - "5001:5001"

  #################################
  # User: The User microservice
  #################################
  user:
    build:
      context: ./
      dockerfile: user.Dockerfile
    image: minhuilee/user:esd
    restart: always
    environment:
      dbURL: mysql+mysqlconnector://is213@localhost:3306/user
      PYTHONUNBUFFERED: 1
    ports: 
      - "5004:5004"
  
  #################################
  # Tutor: The Tutor microservice
  #################################
  tutor:
    build:
      context: ./
      dockerfile: tutor.Dockerfile
    image: minhuilee/tutor:esd
    restart: always
    environment:
      dbURL: mysql+mysqlconnector://is213@localhost:3306/tutor
      PYTHONUNBUFFERED: 1
    ports: 
      - "5006:5006"


  ####################################
  # RabbitMQ: The messaging broker   
  ####################################
  
  rabbitmq:
   image: rabbitmq:3-management
   hostname: esd-rabbit
   restart: always
   ports:
     - "5672:5672"
     - "15672:15672"
   volumes: 
    - rabbitmq_data:/var/lib/rabbitmq
  
  #################################
  # Inbox: The Inbox microservice
  #################################
  inbox:
    build:
      context: ./
      dockerfile: inbox.Dockerfile
    image: minhuilee/inbox:esd
    restart: always
    environment:
      dbURL: mysql+mysqlconnector://is213@localhost:3306/inbox
      PYTHONUNBUFFERED: 1
    ports: 
      - "5002:5002"


  #################################
  # ManageOffer: The manageOffer microservice
  #################################
  manageoffers:
    build:
      context: ./
      dockerfile: manageoffers.Dockerfile
    image: minhuilee/manageoffers:esd
    restart: always
    # depends on:
    #   - inbox
    #   - rabbitmq
    environment:
      dbURL: mysql+mysqlconnector://is213@localhost:3306/manageoffers
      PYTHONUNBUFFERED: 1
    ports: 
      - "5100:5100"



################################################### NOT WORKING BELOW

  
 
  ###################################
  # Error: The Error microservice
  ###################################
  error:
    build:
      context: ./
      dockerfile: error.Dockerfile
    image: minhuilee/error:esd
    restart: always
    depends_on:
      - rabbitmq
    environment:
      rabbit_host: rabbitmq
      rabbit_port: 5672
      PYTHONUNBUFFERED: 1


